#pragma once
// promkit-cpp public API placeholder
// This header will expose init/shutdown, handle getters, and RAII timer in future versions.
// For now, it's a marker to make the build skeleton complete.

namespace promkit {
struct version { static constexpr int major = 0, minor = 1, patch = 0; };
}
